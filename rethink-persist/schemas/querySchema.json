{
	"input": {
		"title": "Query",
		"type": "object",
		"required": ["term"],
		"properties": {
			"term": {
				"description": "JSON query"
			},
			"pipeId": {
				"description": "Nexus pipeId to stream results",
				"type": "string"
			},
			"keepalive": {
				"description": "Keepalive to keep the pipe open",
				"type": "number"
			}
		}
	}
}